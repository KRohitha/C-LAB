#include <stdio.h>

int main() {
   int n, sum = 0, r;
   printf("REGD NO:25331A05F7\n");
   scanf("%d", &n);

   while(n > 0) {
       r = n % 10;
       sum += r;
       n = n / 10;
   }

   printf("%d", sum);
   return 0;
}
