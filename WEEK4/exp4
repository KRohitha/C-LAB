#include <stdio.h>
int main() {
   int n, fact=1;
   printf("REGD NO:25331A05F7\n");
   printf(“enter n:”);
   scanf("%d",&n);

   while(n > 0) {
       fact *= n;
       n--;
   }

   printf("%d", fact);
   return 0;
}
